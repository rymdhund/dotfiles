### Activating the layout

```
xkbcomp -I$HOME/dotfiles/keyboard conf.xkb $DISPLAY
```

### Getting different desktops to recognize the layout

```
cp symbols/sedvorak /usr/share/X11/xkb/symbols/
setxkbmap sedvorak
```

And then add this to /usr/share/X11/xkb/rules/evdev.xml (after the swedish <layout> section).

```
    <layout>
      <configItem>
        <name>sedvorak</name>
        <shortDescription>se dvorak</shortDescription>
        <description>SwedishRymdhund</description>
        <languageList>
          <iso639Id>swe</iso639Id>
        </languageList>
      </configItem>
    </layout>
```
